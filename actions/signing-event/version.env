# Which tuf-ci binary this action runs.
#
# Updated by .github/workflows/release.yml when a version is tagged. The digest is what
# makes pinning the action by commit SHA meaningful: it fixes the exact bytes that will be
# downloaded and executed, so a re-tagged or replaced release asset cannot change what a
# pinned action runs.
#
# TUF_CI_SHA256 is a placeholder until the first release is tagged; the install step will
# refuse to run a binary that does not match it.
TUF_CI_REPO=rf-signing-experiment/tuf-ci
TUF_CI_VERSION=v0.2.0
TUF_CI_SHA256=cb9ea0403879285fbb90ed28066aa0a72afeae293064663f087984d6e04e5898